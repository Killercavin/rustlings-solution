// TODO: Fix the function body without changing the signature.
fn square(num: i32) -> i32{
    num * num
}

fn main() {
    let answer = square(3);
    println!("The square of 3 is {answer}");
}

// this excerise is about function signature, the function signature is the function name, the parameter, and the return type
